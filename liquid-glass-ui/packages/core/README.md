# @liquid-glass-ui/core

本地 private workspace 包，版本 0.1.0-alpha.1，未发布到公共 npm。源码许可 MIT。完整说明见根目录 README 和 docs/。

先在根目录执行 `npm install`、`npm run typecheck`、`npm run build:packages`；不要将离线检查转译当作语义类型检查。tokens.css/styles.css 按根目录示例导入。

组件库默认 CSS，SVG 为受控增强。正式 Chrome / SSR 等验证边界见 reports/verification.md。
