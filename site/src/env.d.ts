/** Replaced at build time from package.json — see `define` in site/vite.config.ts. */
declare const __LG_VERSION__: string;
/** Replaced at build time from the repository's CHANGELOG.md, for the 更新日志 page. */
declare const __LG_CHANGELOG__: string;
