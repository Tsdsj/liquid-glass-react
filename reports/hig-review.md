# 设计审查

最新一轮在最上面。更早的几轮按时间倒序排在后面，没有删——一条「当时为什么这么定」比结论本身更难重建。

---

## 文档站的桌面形态（2026-09-21）

所有者的话是「这桌面效果也太丑了」。不是找缺陷，是**这一页在宽屏上没有形状**——而它是这个库唯一的证据，一个不像桌面应用的桌面组件库文档站，本身就是一条结论。

先量（1680×1000，`#/components/button`）：

| # | 量出来的 | 规则 | 做法 |
| --- | --- | --- | --- |
| 1 | 导航 228×114，栏 260×1000，底下 886px 空的；当前分区的页面列表渲染在这块玻璃**外面**，直接压在页面背景上 | macOS 的源列表是一整块材质跑满窗口高度 | `TabBar` 侧边栏形态变成源列表：分区在上、`accessory` 在分隔线下占掉其余并自己滚动，同一块玻璃 |
| 2 | 标题栏 x=190 w=1300，正文 x=320 w=1300 | 对齐是信息 | 标题栏是固定定位的，侧栏不在它的包含块里；补上等于侧栏宽度的前缘留白，两者成为同一列 |
| 3 | 目录右缘 1510，窗口 1680 | 每个元素都锚在某条边上 | 正文列宽随窗口分档（1000 / 1180 / 1300），目录贴住列的后缘 |
| 4 | 示例格 327px，按钮页一行七个按钮折成三行 | 示例要展示组件，不是展示窄列 | 栅格下限 320 → `min(100%, 420px)` |
| 5 | 窗口顶部只有两个图标按钮 | the-menu-bar | 文档站自己用 `MenuBar` 搭了外观 / 文字 / 辅助功能 / 帮助，每一项都真的做事 |

**三件值得记下来的事。**

其一：**我加了一条行宽上限，量完发现它让事情更糟。** 站点本来就有 `--lg-readable: 672px` 管着正文。我又加了一条 `84ch`，而 `ch` 是按**元素自己的字号**解析的——同一条声明给 12px 的导语 635px，给继承了文档 16px 的卡片 847px，比原来那条 672 还宽。量了，删了。**一个没有先量就加上去的上限，是在凭印象修一件没坏的事。**

其二：**「拿掉一个多余的控件」拿错了。** 菜单栏能装下那五个设置之后，我把偏好弹窗在 ≥1024 隐藏了，七条既有用例当场红——它们都从那个按钮进去。而它们是对的：桌面上菜单栏和设置面板不是重复，菜单栏是快的那条路，面板是那句「这些开关叠加在系统设置之上」唯一放得下的地方。收回。

其三：**一条守卫没有证据，删掉了。** 分区链接一度被挤成两列，我同时加了 `flex-wrap: nowrap` 和给页面列表零 flex 基准两条修法。撤掉前者，包括 1024×560 + AX5 + 没有页面列表的那一页在内，一条都不红——真正的修法是后者。留了那条用例并在里面写明：它是覆盖，不是证明。

---

## 0.0.2 第三阶段第 6 周：命令面（2026-09-21）

审查对象是新写的三样：`MenuBar`、`GlassMenuItem.alternate`、`CommandPalette`。

| # | 位置 | 规则 | 做法 |
| --- | --- | --- | --- |
| 1 | 菜单栏的层归属 | materials：玻璃只在浮起来的导航 / 控件层 | 玻璃在**栏**上，标题是栏上的项；打开的面板是另一块大玻璃，在顶层，不是嵌套 |
| 2 | 菜单项的去留 | the-menu-bar：「**一直显示同一组菜单项**……不可用就禁用，不要从菜单里拿掉」 | `disabled` 在整个菜单和单条命令上都是「仍然画出来」 |
| 3 | 标题 | the-menu-bar：「菜单标题尽量用一个词」 | 文档写明；多词用 Title Case |
| 4 | 快捷键 | the-menu-bar：「支持标准菜单项定义的快捷键」 | 菜单项的 `shortcut` 经 `Kbd` 渲染并带 `aria-keyshortcuts`，与 `useShortcut` 同一次解析 |
| 5 | ⌥ 替代项 | macOS 的 File ▸ Close → Close All | **原地替换**而不是多一行；并写明它永远只是快捷写法，不能有命令只住在那里 |
| 6 | 面板的无障碍模型 | searching + WAI-ARIA combobox | 输入框 `combobox`、列表 `listbox`、高亮用 `aria-activedescendant`，焦点不离开输入框 |
| 7 | 面板与历史 | searching：「显示搜索历史之前先想想隐私」 | 什么都不记；要「最近使用」由应用自己排序并给清除入口 |
| 8 | 键盘模型 | keyboards | 整排一个 Tab 位；方向键在栏上换菜单、在菜单里换项；Escape 关闭并还焦 |
| 9 | 按压态 | 清单「press state on every button」 | 菜单栏标题原来只有 90ms 底色，补了 `transform`（见下） |

**四件值得记下来的事。**

其一：**这一轮唯一的真缺陷是测试逮的，不是我想到的。** 用鼠标点开一个菜单，光标就停在那个标题上；之后按方向键走，浏览器在隐藏一块面板、显示另一块之后会在**原地**补发一个指针事件。把它当成悬停，菜单就被拽回光标底下——按右键，它往右一格又弹回来，最后一个都不开。区分办法是比较坐标：合成事件重复的是上一次的位置，真的移动不会。**「这个元素移到了指针下面」和「指针移到了这个元素上」在事件里长得一模一样，只有坐标能分开。**

其二：**那条用例第一版是假绿的。** 点完立刻按键时，按键赶在补发的指针事件前面，坏的版本照样过。现在点击和按键之间先断言菜单确实开着——**那一行不是整洁，是证据的一部分**，用例里写了这句话。

其三：**一处守卫撤掉之后没有红，所以删掉了。** 菜单在同一次提交里一开一关，我以为关掉的那个会晚一步报上来、把新开的也关掉，于是加了「只有还是它才算数」的判断。撤掉，12 条全绿——React 在那一刻已经认为它是关的，那次 `setOpen(false)` 根本不会往外报。和第 1 周的 `grab`、第 5 周的两条守卫是同一件事，**只是这次的结论是删**。顺手补了一条「点外面关掉」的用例，那条路径此前谁也没测过。

其四：**动效清单报出一条我不会去看的**：菜单栏标题的按压态只有 90ms 的底色变化——纸面上有，眼睛来不及。库里其他所有可按的东西都会在手指底下动一下。补了 `transform`，`motion.spec.ts` 多两条。

---

## 0.0.2 第三阶段第 5 周：表单的基础件，和一个印在屏幕上的承诺（2026-09-21）

这一轮是加东西，所以审查的对象是新写的三样：`GlassCheckbox`、`RadioGroup`、`useShortcut`。

| # | 位置 | 规则 | 做法 |
| --- | --- | --- | --- |
| 1 | 两个新控件的层归属 | toggles · macOS：「开关、复选框、单选按钮用在窗口**内容**里，不要用在窗口边框里」 | 内容层，不是玻璃 |
| 2 | 状态的表达 | toggles：「不要只靠不同的颜色传达状态」 | 开关两态是两个**形状**（勾 / 空），不是两种颜色 |
| 3 | `'mixed'` | toggles：父项管着子项、子项状态不一致时显示 mixed | 走原生 `indeterminate`；按下半选的父项全开 |
| 4 | 键盘模型 | keyboards：Full Keyboard Access | 都建在原生输入上，一行没覆盖 |
| 5 | 快捷键 | keyboards「Standard keyboard shortcuts」 | `useShortcut` 与 `Kbd` 同一次解析；三条「不触发」的规则 |

**三件值得记下来的事。**

其一：**我把 `Kbd` 改过了头，是既有用例把它挡回来的。** 为了让 `mod` 在 Windows 上诚实，我顺手让整个组件在非苹果平台上把符号换成单词——「Ctrl+Alt+Delete」而不是 ⌃⌥⌫。理由成立（PC 键帽上没有 ⌃），但那不是这个组件该替调用方做的决定：写 `keys="ctrl alt delete"` 的人要的就是那几个键。`kbd.spec.ts` 当场红了三条，它是对的。**一个「顺手」的改动越过了它要修的那个缺陷的边界，而边界正是用例画的。**

其二：**两条守卫一开始没有证据，和第 1 周的 `grab` 一模一样。** 撤掉「按住不放只算一次」和「输入框里的裸字母不触发」，用例照样全绿——前者因为 Playwright 的键盘永远发 `repeat: false`（真的产生不了自动重复），后者因为示例里唯一的快捷键带修饰键，那条分支根本到不了。给示例加了一个不带修饰键的 `/`，并用合成事件测 `repeat`，同时在用例里写明合成事件比真输入弱在哪。**撤掉修复也不会红的用例，证明的是别的东西**——这句话这一期已经用第二次了。

其三：**「更大的目标」不一定是更大的目标。** 复选框的隐形 `<input>` 我一开始铺满整行，想的是命中区。其实 `<label>` 本来就让整行可点，而压在文字上的输入框会让文字选不中、吞掉标签里的任何其他东西——Playwright 的报错是直接的：「input intercepts pointer events」。

---

## 0.0.2 第三阶段第 4 周：桌面不是放大的手机（2026-09-20）

这一轮不是找缺陷，是改默认值。库按 iOS 那套表建起来，而在一台有光标的机器上那套表整体是错的。

| # | 严重度 | 位置 | 违反的规则 | 量到的 |
| --- | --- | --- | --- | --- |
| 1 | Major | 全库度量 | pointing-devices：「只在带来价值时才区分指针与手指」；typography · macOS：body 13/16；buttons · macOS：regular 22pt | 桌面上每个控件 44px、正文 17px——一个被画在桌面上的手机界面 |
| 2 | Major | 文档站代码块 | 可读性 | 跟着字号表掉到 **10.1px**（脚注 11，里面的 `<code>` 再乘 0.92）。由新加的 desktop 矩阵行报出 |
| 3 | Minor | `SplitView` 分隔线 | WCAG 2.2 Target Size (Minimum) 24×24 | 抓取区 11px，指针下够不着。同上 |
| 4 | Minor | 分段控件命中区（潜伏） | — | 伪元素画在那个铺满 label 的透明单选框**之上**，吞掉每一次瞄准输入的点击。只在粗指针下画时看不出来 |

还有一条是**在浏览器里看出来的，不是量出来的**：按钮的字号直接读 `subhead`，桌面表里那是 11px，比平台自己的表小（macOS 的按钮标签是正文 13），也比旁边的正文小。所有数字都对，按钮却看起来像被缩过一次。机器问「有没有」，人问「对不对」——这一条只有后者答得出来。

**四件值得记下来的事。**

其一：**这一轮的大部分工作不是加功能，是读既有用例在说什么。** 改完默认值当场红了 15 条，每一条都要分清「它在守一个仍然成立的约定」还是「它在问这还是不是一台手机」。`menu-button` 那条「每个尺寸都保持 44pt 命中区」是后者——它把一条关于**指尖**的规则写成了一条关于控件的规则。现在有 `hit-floor.ts`：两个下限都是真的（手指 44、光标 24），从页面里读，用例不会和样式表对这是什么平台产生分歧。

其二：**一块看不见的命中区制造了一条看得见的滚动条。** 绝对定位的区域撑出容器之后仍然计入页面的可滚动溢出，五个布局页在 RTL 下横向滚动 82px。指针下改成只撑竖直方向——光标是一个像素，需要 44px 宽是因为指尖是圆的。

其三：**一条理由的适用范围，和这条理由本身一样重要。** 输入框里 16px 的下限来自「iOS Safari 聚焦时会缩放页面」，那是关于触摸屏 Safari 的事实。把它留在桌面上的代价当场可见：16px 的行放不进 22px 的盒子，三种字段尺寸里有两种画出来一模一样——一个内容下限披着三个名字。

其四：**页面上印着的数字现在有两组。** 文档站的字段示例原来在占位符里写「36px / 44px / 52px」，那是触摸那一组；对一个用鼠标的读者来说它就是一句假话。现在印的是「触摸 44 · 指针 24」，用例在两套度量下各量一次。

---

## 0.0.2 第三阶段第 3 周：从哪儿长出来，以及一条名单式的无障碍开关（2026-09-20）

| # | 严重度 | 位置 | 违反的规则 | 量到的 |
| --- | --- | --- | --- | --- |
| 1 | **Blocker** | `components.css` 的减少动效段 | 「用户偏好优先」（`AGENTS.md`）；accessibility：Reduce Motion 必须生效 | 一刀切的 `transition: none` 只写了 `.lg-root` 加四个手写组件名。颜色井的选中环、折叠区悬停、分栏分隔线在这个设置打开时照常动 |
| 2 | Major | `anchor.tsx` / `.lg-popover` `.lg-menu` | motion：「menus / sheets / dialogs **morph out of the control** that opened them」 | 朝上打开的面板从距按钮 **16px** 处展开、落在 **10px**——往远离触发它的按钮的方向长 |
| 3 | Minor | `ColorWell` | 选择态变化要看得见 | 选中环是 `box-shadow`，而 `box-shadow` 是那条过渡里唯一没列的属性 |
| 4 | Minor | 分段控件 / 页内标签 / 标签栏 / 页码点 | pointing-devices：指针平台要有悬停反馈 | 四个控件的「未选中项悬停」都写了，当前选中项正好从规则里漏出去。分段控件那条在每个有它的页面上都报了一次，共 63 次 |
| 5 | Minor | `GlassDisclosure` 标题行、`SplitView` 分隔线、`ColorWell` 本体 | 「press state on every button」 | 底色/线色直接换，或者根本没有反应 |

**第 2 条的根因比表面更细**：缩放原点一直是从触发器算的，`data-placement` 也一直是对的——箭头、阴影、所有读它的东西都对。错的只有一帧：`@starting-style` 是在面板不再是 `display: none` 的那一刻读的，也就是 `showPopover()` 那一刻，而属性是之后才写的。**一个在 99% 的读者面前都正确的属性，在唯一一个决定入场方向的时刻是空的。**

**三件值得记下来的事。**

其一：**最值钱的一条不在缺口清单里。** 第 1 条是计划里那句「守住反面：减少动效下这些动画全部归零」逼出来的——断言写下去当场就红。一份需要每加一条过渡就回来补一次的名单迟早是错的，而且错得没有声音：被记住的那几个组件，正是你会先去检查的那几个。**反向断言和正向断言不是同一件事的两种写法。**

其二：**这份清单自己骗了自己四次**，每一次都让它的读数更大也更没用——探针没滚进视口（「没动」其实是「没碰到」）、`activate` 问的是整个文档所以恒真、反馈落在兄弟节点上而它只看子树、文档站自己的按钮也被算进来。1255 → 2。**一份大部分内容是「正确行为」的报告没有人会读**，顶上那个数字比没有数字更糟，因为总会有人引用它。计划里写的「行按压」就这么凭空出现又凭空消失：行的底色过渡一直都在。

其三：**修一处动效，撞了另一处动效。** 给选中段加悬停时我先用了缩放，而缩放写在 `transform` 里——悬停开一条 transform 过渡会顶掉正在跑的那条，指针扫过控件就会让滑行重新开始。第 1 周那套 `interruptible.spec.ts` 在一分钟内红了。**按下可以改变胶囊要去哪里；指针从上面路过不可以。**

---

## 0.0.2 第三阶段第 2 周：来去成对（2026-09-20）

清单先于结论：`pnpm test:motion` 在每个组件页上逐个控件做悬停、按下、激活、聚焦，问 `getAnimations({ subtree: true })` 有没有东西在动。有状态变化而什么都没动的，就是一条发现。

| # | 严重度 | 位置 | 违反的规则 | 量到的 |
| --- | --- | --- | --- | --- |
| 1 | Major | `ToastProvider` | motion：「materialize, not fade」——来去都要有 | 关闭之后节点当帧从 DOM 消失，`getAnimations()` 为空。四条关闭路径（按钮、Escape、倒计时、被挤出队列）各走各的 |
| 2 | Major | `Banner` | 同上 | 进场退场都没有。样式表里那条 `transition` 是给「往上一甩」的回弹用的，不是进出 |
| 3 | Major | `GlassTabs` | 同上 | 离开的面板只有 `hidden`，而 `display: none` 会当场结束动画 |
| 4 | Minor | `GlassBadge` | 数值变化要能看出来变了 | 二变三在两帧之间完成，没有任何提示 |
| 5 | Major | `SplitView` / `Inspector` | 布局变化用动效解释「东西去哪了」 | 栏是条件渲染的：260px 到没有，中间零帧 |

### 清单本身的读数，以及它还不能回答的

40 页、2640 个探针、1271 个「触发了但什么都没动」。**这不是 1271 个缺陷**，而且说清楚为什么，比这个数字本身重要：

- 「聚焦按钮什么都没动」120 条——焦点环是 `outline`，系统自己的焦点环也是当场出现的。不动是对的。
- 「悬停/按下卡片」各 47 条——卡片是内容层，不该动。
- `activate` 这一维**一条发现都没有**，因为它问的是整个文档，而页面上只要有一个不确定进度条在转，答案就永远是「有东西在动」。这一维目前等于没测。

所以这一版清单能可靠回答的只有一件事：**某个具体控件的某个具体状态变化有没有动**。把它从 1271 条噪音收窄到一张能逐条看的表，是第 3 周的事：`activate` 要改成只看新出现的那几个动画，`hover` / `focus` 要按「这一层该不该动」分开问。先记在这里，免得下一次读到这个数字的人以为它是缺陷数。

**三件值得记下来的事。**

其一：**跑不完的清单等于没有清单。** 第一版只在最后写报告，每类元素探 6 个、每页不封顶，二十分钟超时、`reports/motion.json` 一个字都没有。现在每扫完一页就重写一次，并且每页最多 24 个探针。一份扫到一半的清单有用，一份扫不完的没有。

其二：**我用 `git checkout --` 做撤销验证，把自己的改动删了。** 循环里每轮撤销一处修复、跑用例、再「恢复」——而恢复写的是 `git checkout -- <file>`，它恢复到的是 HEAD，不是我的工作副本。两个文件的全部改动就这么没了，而且因为第二轮开始所有修复都不在了，输出看起来像是每一条撤销都让半个套件变红，差一点被当成结论写进报告。重做之后改成先 `cp` 到 `/tmp` 再从那里恢复，结论才是干净的：五处修复各自只让它该让的那几条变红。**一个自己会改动工作区的验证程序，不是验证程序。**

其三：一条过渡挂在什么条件下，和它播什么一样重要。分栏那条 `flex-basis` 过渡无条件写上去之后，连「读者正在拖分隔线」也被它抓住了——侧栏落后指针 280ms。`split-view.spec.ts` 里两条既有用例当场红，这正是回归用例存在的意义。**读者正在设的值必须和他们一样快；只有东西来和去才值得看。**

---

## 0.0.2 第三阶段第 1 周：动效可打断（2026-09-20）

所有者在真实浏览器里报的：快速连点可拖动控件的选项，动画被跳过，直接闪到目标；不止一个组件。

| # | 严重度 | 位置 | 违反的规则 | 量到的 |
| --- | --- | --- | --- | --- |
| 1 | Major | `pull.ts`，以及所有用它的控件 | motion：「Let people cancel motion」——可打断的意思是**从当前位置改道** | `pointerdown` 就盖 `data-pulling`，样式表在这个属性下把 `transform` 移出过渡列表，**正在跑的过渡被取消**。逐帧：透镜从 45 滑向 0，滑到 51.3 时按下另一项，下一帧 0——一步 51.3px，而整段顺畅滑行每帧最多 9px |
| 2 | Major | 同上 | 同上 | 同一帧的偏移计算把「指针 − 透镜静止中心」整段写成偏移：按住不动，分段控件的透镜瞬移 **45px**，侧边栏 **92px**。`GlassTabs`、`TabBar` 两种形态、`PageControl`、`GlassSwitch` 全中 |
| 3 | Minor | `rubber()` | — | `limit: 0`（页码点）时算出 `NaN`，一直被当成 `--lg-shift-x: NaNpx` 写进 DOM |

修在手势那一层，不逐个组件打补丁：按下什么都不写、什么都不标，直到指针真的移动超过 4px；控件用新的 `grab` 说明「按在哪里才算抓住了它」；接管的那一帧在盖属性的前后各量一次位置，把差值当成起始偏移交回去，元素从它看起来所在的位置继续。

**两件值得记下来的事。**

其一：识别阈值吃掉的那几像素，留着和一帧还清都不对——留着元素会永远落后手指（慢拖 8px，快甩半条轨道），一帧还清就是同样大小的跳变。改成 50ms 内还清之后，「透镜 1:1 跟手，误差 2px 以内」那条既有用例**一个字都没改**。一开始我把容差从 2 放宽到 6 并写了理由，那是在给自己的实现找台阶。

其二：`grab` 差一点是一段没有证据的代码。撤掉它，八条用例里有七条照样全绿——因为阈值本身已经挡住了静止按下的瞬移。真正需要它的场景是**按下未选中项再拖动**：没有它，胶囊会被带离它所指的选中项，实测停在 52，而选中的那一项在 0。补上那条用例之后撤销才会红。**一条撤掉修复也不会红的用例，证明的是别的东西。**

还差一口气：改道之后弹簧从零速度重新起步——位置连续，速度不连续。真正的速度接管要把透镜的位移换成 JS 弹簧，记在第三阶段的后续。

---

## 上一轮审计的处理结果（2026-09-20）

下面那 17 条**全部修了**，每条都有一个会因为撤掉修复而变红的东西守住它：可测的进浏览器用例，结构性的进 `scripts/check-props.mjs`（构建闸门），两份必须一致的调色板进 `tests/core/stylesheet.test.mjs`。

自动化：71 项核心测试、7 项 SSR、299 项 Playwright（真实 Google Chrome，+13）、13 项开发模式（+2）、18 项跨引擎、440 格矩阵 0 条发现。

| # | 处理 | 守住它的 |
| --- | --- | --- |
| 1 | `tinted` 改用**墨色**而不是强调色本身：新增 `--lg-ink-toward` / `--lg-ink-amount`，在用到的元素上混色 | `appearance.spec.ts`「a tinted button's label clears AA against its own wash」，撤掉后浅色测得 2.59:1 |
| 2 | 护栏改为量**渲染后的那一对**：`getComputedStyle().color`，对上色调层 + 背景 + 每层祖先合成到不透明 | `warn-probe` 新增 `tint-contrast` 区块，`warnings.spec.ts` 守住它响且只响一次 |
| 3 | `tertiary` 的两处演示改成照它的用途写（占位符 / 停用态），不再拿它当正文 | 排版页文案；11px 那一档改用 secondary |
| 4 | 属性路径换上**整份**高对比调色板，与媒体查询逐字一致 | `stylesheet.test.mjs` 比对两个块的声明集合；`a11y.spec.ts` 断言开关真的换掉了 `--lg-blue` |
| 5 | `GlassProvider` 把显式覆盖写到 `<html>`：`data-lg-motion` / `data-lg-transparency` / `data-lg-contrast` | `a11y.spec.ts` 断言属性上了 `<html>`，且 `.page-enter` 的动画名变成 `none` |
| 6 | `.doc-related-link` 补 `overflow-wrap: anywhere` + `min-width: 0` | `docs.spec.ts` 在 390px/AX3 下跑三页，撤掉后横向溢出 35px |
| 7 | `.lg-segment` / `.lg-tab` 补命中区，**只撑竖直方向** | `touch.spec.ts` 以控件中心 ±21px 做命中测试 |
| 8 | 十一个浮层页各给一个示例 `backdrop: 'both'` | `check-props.mjs` 拒绝构建「浮层组里没有一个示例能在照片上看」的页；`docs.spec.ts` 抽查开关真的在 |
| 9 | 偏好面板三行改成 `GlassSwitch` 自己的可见标签槽；`Form` 页那条理由改写 | `docs.spec.ts`「no label on this site contains another label」 |
| 10 | 随第 4 条关闭：高对比下白字压通知红 **4.57:1**、压强调蓝 **4.58:1** | 同第 4 条 |
| 11 | 修的是 `Picker` 不是面板：**两个选项在任何宽度下都并排** | `warnings.spec.ts` 在 390px 下跑四页，要求一条告警都没有 |
| 12 | 两条死链改对，并把「related 指向不存在的页」变成构建失败 | `check-props.mjs` |
| 13 | `NavigationBar` / `NavigationStack` 层级放开到 6，`ListSection` 新增 `headingLevel`；示例统一降到 4 | `docs.spec.ts`「a heading inside an example sits below the example's own title」 |
| 14 | 小号输入框的内边距让步，36 成为真的 36 | `docs.spec.ts` 比对占位符里写的数和渲染高度 |
| 15 | 页码点的说明改成只承诺竖直方向 | 文案 |
| 16 | 文字大小加到 **AX5**（布局规则的注释量的就是这一档） | `docs.spec.ts` 切到 AX5 并断言不横向溢出 |
| 17 | `useKnobs` 真的有 `reset`，动过之后出现「恢复示例原样」 | `docs.spec.ts` 改一个旋钮、复位、断言回到初值 |

**两条值得单独记：**

第 2 条改完当场逮到站点自己的绿色确认按钮 `tint="#1a7f37"`：玻璃的色调层以 0.92 不透明度压在浅色页面上，绿色被提亮成 `rgb(43 136 70)`，白字实测 **4.45:1**。旧算法给同一个按钮 5.6:1 并放行。**一条量错对子的护栏比没有护栏更坏**，这就是它的样子——不是理论上的。

第 4 条顺带挖出一个从来不可能匹配的选择器：徽标的高对比边框写的是 `.lg-root[data-contrast="more"]`，而材质盖的章是 `data-lg-contrast`。这条规则从写下那天起就没生效过，而它旁边的媒体查询版本一直是对的——所以没有人看得出来。

还有一处是自己的用例先错了：分段/标签的命中测试第一版接受「命中点落在包含该控件的轨道上」，于是**在完全没有命中区的情况下也是绿的**。改成必须命中控件本身或它内部，撤掉修复才真的变红。按下轨道而不是按下分段，选中的是什么都没有。

---

## 0.0.2 文档站演示审计：把页面上的每句断言拿去量（2026-09-20）

这一轮只看一件事：**文档站上那 40 个组件页、约 120 个示例，自己说到的有没有做到。**

上两轮已经把「有没有溢出、够不够得着、有没有名字」交给了 440 格矩阵，把「好不好看」交给了人眼。这一轮问的是第三种问题——页面上写着「低于 4.5:1 就告警」「触摸时命中区是 44」「label 套 label 非法」，那就把这些话一条条拿到浏览器里量。**断言本身就是可测的用例，而之前没有人把它们当用例跑过。**

量的方式：真实 Chrome，桌面与 375×812 两种宽度，浅色与深色，四项偏好开关各开一遍，文字大小到 AX3。对比度按祖先链逐层合成到不透明再算，不拿半透明的 `rgba` 当实色——第一遍就是这么算错的，`variant="gray"` 被误报成 4.38:1。

### Blocker

| # | 位置 | 违反的规则 | 量到的 |
| --- | --- | --- | --- |
| 1 | `GlassButton variant="tinted"`（`button-variants`、`button-tint`） | HIG accessibility：17pt 以下正文 **4.5:1** | 标签是强调色压在**同一个强调色的 16% 淡底**上：浅色 **2.75:1**，深色 **2.55:1**；`tint="#8250df"` 的「升级」**2.87:1**。而这条示例自己的说明写着「低于 4.5:1 就告警」——告警没响 |
| 2 | 同上，`button.tsx:98-106` 的开发期护栏 | 护栏量错了对子 | 它比的是 `tint` 对 `tintContrast ?? '#fff'`。`tinted` 的标签**不是白的**，是强调色本身。于是护栏量了一对屏幕上不存在的颜色（`#8250df` 对白 ≈ 5.6:1）并放行，真正渲染的那一对 2.87:1 从来没被看过一眼 |
| 3 | `Text tone="tertiary"`（`text-scale` 的 caption2、`text-tone` 的「更次要」） | 同上 | 浅色 **2.37:1**，深色 **2.25:1**；其中一处是 11px。教排版的那一页，自己示范了一档读不出来的颜色 |

第 1、2 条是同一件事的两半，但要分开记：光把颜色调深是治不住的，因为**护栏还会继续放行下一个人**。`components.css:486` 里 `background: color-mix(in srgb, var(--lg-accent) 16%, transparent); color: var(--lg-accent)` 决定了这个变体的对子永远是「强调色 vs 强调色的淡底」，护栏就应该量这一对；现在它量的是 `glassProminent` 的对子，只是被写在了所有变体共用的那段 effect 里。

第 3 条挑明一点：Apple 的 `tertiaryLabel` 本来就是给占位和停用态用的，低对比是设计意图。问题不在这个 token 存在，在于**演示把它当成一段要读的正文**，还挑了最小的字号去示范。

### Major

| # | 位置 | 违反的规则 | 量到的 |
| --- | --- | --- | --- |
| 4 | 显示偏好里的「增强对比度」 | HIG accessibility：默认达不到 4.5:1 时，**至少要在系统「增强对比度」打开后给出更高对比的配色** | 开与关，三处测点一个数都没动：`tinted` 2.75 → 2.75，`tertiary` 2.37 → 2.37，徽标 3.57 → 3.57。高对比调色板写在 `tokens.css:388` 的 `@media (prefers-contrast: more)` 里，**只挂在系统开关上**；站内这条路走 `[data-lg-contrast="more"]`（`tokens.css:409`），只换了 `label-secondary / label-tertiary / separator` 三个值，**没有换调色板**。而且这个属性由 `material.tsx:226` 写在**每个玻璃根节点**上（实测 25 个），从来没写到 `<html>`，所以内容层的文字根本取不到它 |
| 5 | 显示偏好里的「减少动效」 | 同上，减少动效要生效 | 打开之后 `.page-enter` 仍然跑 0.36s 的位移淡入（`app.css:129`），CSS 过渡时长不变。站内这个开关只进 React context，进不了 CSS——`app.css:387` 的 `prefers-reduced-motion` 同样只认系统。面板自己写着「这些开关叠加在系统设置之上」，三个开关里只有「减少透明度」真的做到了（实测 `backdrop-filter` 归零） |
| 6 | `.doc-related-link`（相关组件卡片） | Dynamic Type 要的是重排，不是横向滚动 | AX3 + 375px 下，`button` / `switch` / `picker` / `tabs` / `page-control` 五页整份文档横移 **50px**。凶手是「分段控件 GlassSegmentedControl」这一条——341px 的盒子里 `scrollWidth` 408。`app.css` 已经为完全相同的原因在 6 个地方加了 `overflow-wrap: anywhere`（页头、`.doc-a11y`、`.plain-list`、`.subnav-link`、`.outline-link`、`.props-type`，注释里还留着「AX5 量到 57px」），**唯独漏了这一处**。现场补一行 `overflow-wrap: anywhere` 验证过：425 → 375 |
| 7 | `.lg-tab`（36pt）与 `.lg-segment`（常规 36pt / 紧凑 28pt） | HIG buttons：「a button needs a hit region of at least 44x44 pt」 | `(pointer: coarse)` 下两者都**没有**任何命中区补偿。库里 `.lg-button`（`components.css:459`）、`.lg-page-dot`、`.lg-split-divider`、`.lg-tab-link` 都补了，这两个漏了。`GlassButton` 的辅助功能条目还明写着「即使按钮看起来更小，可点范围也会补足到 44×44」——同一套规矩没有落到它的兄弟控件上。紧凑分段 28pt 正好卡在 HIG 表格的最低值上，而文档站自己的显示偏好面板用的就是紧凑分段 |
| 8 | 全部 11 个「浮层」组件 | materials：玻璃的意义在于**让底下的内容透出来** | 约 120 个示例里只有 **13 个**能在照片背景上看；40 个组件页里只有 **7 页**提供「图片背景」切换。浮层组 0 页——气泡面板、菜单、底部面板、警告框、操作表、对话框、轻提示、横幅，一个都没有。这些恰恰是「浮在内容之上」这句话唯一说得通的地方。`ComponentPage` 里 `hasMediaExample` 为假时连切换开关都不渲染，读者想试也试不了 |
| 9 | `preferences.tsx:40-45` | 站点自己的 `Form` 页（`content.tsx:576`）写着：「label 套 label 非法」 | `.pref-row` 是 `<label>`，里面包着 `GlassSwitch`，而 `GlassSwitch` 自己就渲染 `<label htmlFor>`（`switch.tsx:53`）。三处，逐字踩中自己禁止的写法。顺带：那句话给的理由「浏览器的答复是外面那个直接失效」在 Chrome 里是**反的**——实测点外层文字，开关照样从 false 翻到 true。结论对，理由不对，而一个以「每句话都能验证」立身的站点，错的理由比错的写法更贵 |
| 10 | `GlassBadge` | 同 4.5:1 | 12px 白字压在语气色上：通知 **3.57:1**，强调 **3.52:1**，浅深两色一致，「增强对比度」也抬不动。Apple 自己的红底白字徽标确实在这个量级，但这一页的说明正好在讲「颜色不能是唯一信息」 |

第 4、5 条要合起来看，因为它们是同一个结构问题：**站内的三个无障碍开关只走到了 React 这一层，没走到 CSS 这一层。**「减少透明度」之所以是对的，纯粹因为玻璃材质本来就在 JS 里读 policy；另外两个要改的东西在 CSS 里，就断了。这不只是文档站的毛病——`GlassProvider` 的 `contrast="more"` 和 `motion="reduced"` 是**公开 API**，任何一个把自己的应用内设置接到这两个属性上的人，拿到的都是同样的空转。最短的修法是把三个状态和 `theme`、`textSize` 一样写到 `<html>` 上，让 CSS 里每一处 `@media` 都配一条属性选择器。

第 8 条是这一轮里最花钱、也最值得的一条。它不是 bug，是**取舍**：把玻璃只放在纯色台面上，每张截图都干净，代价是这个库最想证明的那件事——玻璃会跟着背后的内容变——在文档站上几乎无处可看。至少让浮层组也能切到图片背景，成本只是给每个示例加一个 `backdrop: 'both'`。

### Minor

| # | 位置 | 违反的规则 | 量到的 |
| --- | --- | --- | --- |
| 11 | `KnobPanel`（`knobs.tsx:37`） | 库自己的开发期告警 | 两选项的 `select` 旋钮收成 `GlassMenuButton`，控制台每页都念一遍「has 2 items…below about three items plain buttons show more for less effort」。`list`、`segmented-control`、`progress`、`picker`、`toolbar`、`navigation-stack`、`scroll-edge`、`tooltip` 八页可复现。参考实现踩自己的 lint |
| 12 | `navigation.tsx:569`、`overlays.tsx:1150` | 交叉引用 | `related` 写了 `'nav-bar'`（实际是 `navigation-bar'`）和 `'screen'`（不存在）。两条相关链接被静默丢掉，控制台一直在报，没人收 |
| 13 | `text`、`navigation-bar`、`navigation-stack` 三页 | 标题层级 | 示例内部的 `as="h3"` / `headingLevel={3}` 和页面自己的示例标题**同级**。`navigation-stack` 上读屏按标题跳，听到的是「设置、设置、基础用法、调整属性、收件箱…」——示例里的样例标题和页面目录混成一份，而右侧目录里只有后者。`NavigationBar` 的 `headingLevel` 只到 3，示例想让开也让不了 |
| 14 | `field-sizes`（`fields.tsx:92`） | 演示里的数要是真的 | 小号输入框占位符写着「36px」，实测渲染 **38px**（`components.css:996` 的 `min-height: 36px` 加上边框）。标准 44、大号 52 都对得上，只有这一个差 2 |
| 15 | `PageControl` 辅助功能条目（`navigation.tsx:632`） | 同上 | 「触摸时命中区是 44」只有**竖直方向**成立。`components.css:921` 的 `::after` 是 `width: 100%; height: 44px`，横排时实测 **18 × 44**。取舍本身合理（点挨得近，44 宽会互相压），但话说满了 |
| 16 | `preferences.tsx:36` | Dynamic Type 要测到 AX5 | 文字大小只给到 **AX3**，而 `app.css` 的注释里留着两处「Measured at AX5」。测过的档位，读者调不到 |
| 17 | `knobs.tsx:53` | 注释与实现 | 「Reset puts the page back to the example as it is written」——`useKnobs` 只返回 `values` 和 `set`，没有 reset，界面上也没有 |

### 先修哪一条

**按顺序：2 → 4 → 6 → 7 → 8。**

先修护栏（第 2 条）而不是先修颜色（第 1 条），因为护栏是这个库真正的卖点——它敢在文档里承诺「开发模式会量这一对的对比度」。一条量错对子的护栏比没有护栏更坏：它让每个下游调用方都以为自己被看过了。改完护栏，第 1 条会自己浮出来，连带把别人 app 里的同类问题也一起浮出来。

第 4 条紧随其后，因为 HIG 在对比度这件事上留的台阶就是它：**默认可以不到 4.5:1，前提是「增强对比度」能把它救回来。** 现在这级台阶是空的，于是第 1、3、10 三条从「和 Apple 一样的取舍」变成了「没有退路的失败」。修好它，徽标和强调色按钮这类 Apple 自己也在做的选择就重新站得住了。

第 6、7 条各是一行 CSS，且都是「已经想明白的规矩漏了一处」，性价比最高。

第 8 条不急，但它决定这个站给人的第一印象。现在读者翻完 40 页，看到的是一套很克制、很正确、但**几乎没有背景**的组件——而背景正是 Liquid Glass 全部的意思所在。

---

## 0.0.2 外观复查：人眼先看，再换算成数（2026-09-20）

上面那轮的 440 格矩阵是 0 条发现，而所有者在真实屏幕上一眼看出五条。这不是矩阵失职——它问的是「有没有溢出、够不够得着、有没有名字」，**没有一条规则在问「这个界面好不好看、读不读得清」**。这一节记下来，是因为它划出了自动化的边界在哪。

五条都换算成了可量的数，`tests/browser/appearance.spec.ts` 守住：

| # | 严重度 | 位置 | 违反的规则 | 量到的 |
| --- | --- | --- | --- | --- |
| 1 | Major | 全部玻璃面 | 指针光晕与按压闪光在两种外观下用同一个白色强度 | 深色下 26% 白光把整块面板点亮。改为按外观分开，深色约为浅色的 1/3 |
| 2 | Major | 浅色的全部顶层表面 | 满亮度纯白 + 60% 的次级灰 | 次级正文压在分组表面上只有 **4.43:1**，低于 AA。表面降三点，次级灰提到 74%，测得 4.9:1 |
| 3 | Major | `GlassSegmentedControl` / `GlassTabs` | 「选中项必须一眼看得出」 | 白胶囊压白轨道，实测差 **5/255**；加阴影只多三点。轨道改为压暗的凹槽后差 **20/255** |
| 4 | Minor | `DisclosureGroup` | 展开要有过渡 | 组件一直在量高度并写出 `--lg-disclosure-height`，而没有任何规则读它——箭头转了，内容一帧就出来。改用 `interpolate-size` + `::details-content` |
| 5 | Minor | 文档站首页 | 布局 | 折射开关比它控制的演示还宽，两块对不齐；演示自己缩窄了（有宽高比又有高度上限的 grid 子项会**反过来用高度算宽度**） |

第 3 条值得单独记：**最先想到的修法是错的**。把胶囊调亮、把影子加重，都是在处理「胶囊」，而问题出在「轨道」——两块都是白的时候，再怎么修其中一块也只能多出几个点。系统自己的分段控件是一道灰沟槽加一个抬起的白滑块，凹的那一半才是让选中项看得见的那一半。

也因此改了一条既有用例：`gesture.spec.ts` 里「浅色页面比卡片暗一档」原本硬编码卡片必须是 `rgb(255, 255, 255)`。那个精确值和这条用例要说的事无关，纯白恰恰是刺眼的来源。

---

## 0.0.2 第 6 周：全库复审（2026-09-20）

这一轮的分工和 alpha.6 那次不一样：**结构性的部分交给机器**。`tests/browser/matrix.spec.ts` 把 40 个组件页 × 11 种设置（浅深、四项系统偏好、强制颜色、LTR/RTL、AX5、以及会互相影响的三对组合）跑成 440 格，问的是同一组没有品味成分的问题——有没有横向溢出、命中区够不够、图标按钮有没有名字、字号有没有低于 11、玻璃面有没有塌成零、控制台有没有报错。**440 格，0 条发现。**

人只看机器看不到的那一类。这一轮实际改的四条，全部来自新写的 `layout-matrix.spec.ts`（布局容器 × 四个宽度 × LTR/RTL × 默认/AX5）和逐条复核：

| # | 严重度 | 位置 | 违反的规则 | 处理 |
| --- | --- | --- | --- | --- |
| 1 | Major | `SplitView` | 栏的 flex 基准是刚性的，容器比「侧栏 + 检查器」窄时**分栏视图溢出它自己的盒子**——实测 320px 容器里溢出 161px。布局容器溢出自身，父级完全不知情 | 栏改为可收缩基准，内容栏基准归零先让；见 `docs/api.md` |
| 2 | Major | `SplitView` | 视图比自己的最大值还窄时，`aria-valuenow` 报出 400 而画出来是 304——读屏听到一个没有人拥有的宽度 | 量自己的宽度，扣掉检查器、分隔线和内容栏的 160px 下限，取较小者；`aria-valuemax`、`aria-valuenow` 与画面始终同一个数 |
| 3 | Major | `TabBar` | AX5 下 390px 屏幕上四个标签是 400px 的文字，而栏是 `inset-inline: 0`——整份文档横移 69px。Dynamic Type 要的是重排 | 标签行可换行，栏宽封顶。换行才是重排：只封顶的话标签会被挤成一列一个字，正好挤给了要求大字号的那些人 |
| 4 | Minor | `NavigationStack` | 压栈动画从侧面 16px 滑入，期间栈比自己宽 16px；满屏的栈会让页面每次导航都闪一次横向滚动条 | `overflow-x: clip` 收住这段位移（`clip` 不建立滚动容器，栈里的吸顶栏照常工作） |
| 5 | Minor | `ColorWell` | 快捷色之间 4px，低于「控件之间至少 8pt」，两个焦点环会贴在一起 | 改为 8 |

另有一条不在布局矩阵里、由用例逮到的库缺陷：**`SearchField` 的高亮会被调用方引起的重渲染清掉**。「建议列表变了就清掉高亮」这个 effect 依赖的是数组的**引用**，而筛选归调用方，所以 `suggestions` 几乎总是每次渲染新建——按下 ↓、高亮出现、下一帧换成新数组、effect 又把它清了。这是一场竞态，所以它看起来大部分时候是好的。改为依赖列表的**内容**。

复核通过、没有改动的：内容层无玻璃（新增的 `ColorWell` 与 `Form` 都在内容层，`Banner` 是大玻璃因为它是面板不是控件）；`Banner` 里的按钮走共享表面，没有玻璃叠玻璃；三个新组件的四项系统偏好、强制颜色与 RTL 都在矩阵里跑过；`Picker` 的形态由尺寸类别决定而不是设备类型；颜色只来自 token（静态检查守住）；无 `will-change`、无 `100vh`、hover 全部带指针门（静态检查守住）。

自动化证据：70 项核心测试、7 项 SSR、272 项 Playwright（真实 Google Chrome）、11 项开发模式、18 项跨引擎（WebKit 与 Firefox）、440 格矩阵 0 条发现。单机。

---

## 设计审查 — 0.2.0-alpha.6

对照 Apple 的人机界面指南与 Liquid Glass 指南，把 27 个组件逐个过了一遍，以及全站的分层、色彩、排版、形状、动效、无障碍与 Web 实现七个维度。

自动化证据：65 项核心测试、3 项 SSR 测试、84 项 Playwright 用例（真实 Google Chrome）、16 项跨引擎用例（WebKit 与 Firefox）。单机。日期：2026-09-15。

---

## 〇之〇、alpha.5：全面审计

先修掉报告的缺陷：**跨到另一个选项的那一帧，高亮块飞出整个框**。切换选项换掉了槽位，而这一刻的拖动位移是照着旧槽位算出来的，两者相加把高亮块扔出一整段——实测最远到轨道外 25px，下一帧才纠正。槽位改为在变更时同步把差值吃进位移（同一次写入，且在绘制之前）。顺带两处：按下即选中导致点击时高亮块瞬移（改为交给标签自己的点击，于是会滑过去），以及槽位变化被误当作手指猛动、让玻璃绷紧一帧。

然后按 Apple-Style-Review 的清单把 27 个组件和文档站整个过了一遍：

| # | 严重度 | 位置 | 违反的规则 | 处理 |
| --- | --- | --- | --- | --- |
| 1 | Blocker | 气泡面板 / 菜单 / 底部面板 / 对话框 / 警告框 / 操作表 / 轻提示 / 工具栏组里的分段控件、开关、搜索框 | 玻璃叠玻璃。浮层已经是 `blur(40px)` + 0.9 填充，里面的控件又叠了自己的 `blur(14px)` + 0.84 填充——白花一个合成层，还让控件和面板糊成一片 | 小玻璃放在共享表面上自动转为实心填充控件（系统在面板里就是这么画的）；大玻璃是容器本身，永远保留自己的材质 |
| 2 | Major | 全部 18 条 `:hover` | 没有 `@media (hover: hover)`。触屏上点一下，那个控件就一直亮着，直到点别处 | 逐条加指针门，并用静态检查守住 |
| 3 | Major | 概览、接入指南 | 写着「Chrome 和 Edge 上有完整折射」，但默认渲染方式是 CSS、`enableSvgAuto` 默认关——文档站自己一个折射面都没有 | 改成「默认磨砂，折射要自己打开」 |
| 4 | Minor | `data-variant="danger"` | 对应一个早已不存在的变体；也是文件里仅剩的两个非语义色值，没有深色与增强对比度的对应值 | 删除，并用静态检查守住颜色只来自 token |
| 5 | Minor | 融合层 | 增强对比度下样式表把它藏了，但每次按压 rAF 循环照样启动 | 纳入开关条件 |
| 6 | Minor | 搜索框清除按钮、操作表行 | 没有按下态 | 补上 |

复核通过的：内容层无玻璃、无横向溢出、字号下限 11px、图标全部 24×24 自绘、命中区、焦点环单层（按钮点击不亮，输入框按平台惯例亮但只亮容器那一层）、四项系统偏好（减少动效关过渡并卸掉融合层，强制颜色与增强对比度关掉背景滤镜和高光边）、从右到左无溢出、滚动边缘每个滚动区一组、无 `will-change`、无 `100vh`。

**这条留给以后**：共享表面的规则原本只有按钮遵守，因为只有按钮显式问过 `useSharedSurface()`。规则写在文档里而不是写在材质引擎里，就只有记得问的组件会遵守它。

---

## 〇之一、alpha.4：反馈回路

alpha.3 让控件真正跟手之后，冒出一个更基本的问题：**按住不动，控件也在抖**。

拖动需要知道高亮块「原本该在哪」——它的做法是量出当前位置，再减掉自己刚写进去的位移。这在数学上成立，前提是量出来的位置只等于「槽位 + 位移」。但 CSS 的独立 `scale` 属性作用在 `transform` **之前**，所以写在 `transform` 里的槽位偏移会被当前形变乘一遍：量出的原点带着 `(scale − 1) × 槽位偏移` 的误差。误差进入下一帧的位移，位移改变速度，速度改变形变，形变又改变误差——一个每帧震荡的闭环。侧边栏里越靠下的项槽位偏移越大，所以抖得最凶，这也正是它先被看见的原因。

alpha.2 之所以没暴露：那时位移只在收到 `pointermove` 时算一次，而且形变几乎立刻顶到上限并停住——误差是个常数，只是让透镜偏了一点，不会震荡。alpha.3 把循环改成每帧运行、形变随速度连续变化，常数误差就变成了反馈。

处理：槽位、拖动位移、形变合成同一条 `transform`，缩放放在链条最后，三者彼此独立；`useSelectionLens` 因此改写 `--lg-slot-*` 而不是 `transform`。开关旋钮有同一个次级问题（行程被形变放大），一并改掉。

同时补上标签栏与侧边栏的拖动切换：此前高亮块能被拖走却决定不了任何事。现在与分段控件一致——滑过哪一项就切到哪一项，松手前已经切好，而单纯按一下松开仍然走链接自己的点击。

**这条留给以后**：任何「读回自己刚写的值」的循环都要能证明读和写在同一个坐标系里。这里的教训是合成顺序，不是三角函数或时序。

---

## 〇、alpha.3：手势与首帧

这一轮报告的六个问题都不在静态截图里，全都发生在帧与帧之间。

| # | 现象 | 根因 | 处理 |
| --- | --- | --- | --- |
| 1 | 移动到位后顿一下，然后突然变样 | 拖动期间由融合层代替高亮块作画，结束时在一帧之内换回来。两种画法本来就不完全一致，于是看得见 | 两者由同一个变量互补驱动，交叉淡出 200ms；淡到零后再多等一帧才撤掉图层 |
| 2 | 浅色模式下面板里的字几乎不可见 | 共享表面上的按钮没有自己的材质层，`glassProminent` 的强调色画在装饰层上，于是白字落在完全透明的背景上 | 共享渲染下主操作按钮改用自身背景上色；破坏性主操作同理 |
| 3 | 浅色模式整片白得刺眼 | 页面底色 `#f2f2f7` 与卡片白只差一档，演示台又和页面同色，三层挤在同一个亮度上 | 文档站底色退到 `#e8eaef` 并加一层极淡的渐变；卡片、演示台、页面拉开层次 |
| 4 | 有些组件几乎拖不动，拖动时冒出网址浮标 | 导航项是 `<a>`，按住拖动触发浏览器原生拖拽：既画出网址浮标，又用 `pointercancel` 掐断了手势 | 链接标为不可拖拽；`attachPull` 在手势进行中拦截 `dragstart`；控件外壳统一禁用文本选择 |
| 5 | 拖动不真实，而且有规律性 | 两处：位移**永远**被橡皮筋限制在十几像素内，所以只是朝手指歪一下；形变一开始拖就顶到上限，每次都一样 | 新增 1:1 行程区间，区间内完全跟手、出界才变沉；形变改由速度与「被拽住的程度」共同决定，顺着走几乎不变形 |
| 6 | 每次刷新，带拖动的按钮回弹一次 | 高亮块第一次落位本就是从无到有，却被当作一次状态变化播了动画 | 首次落位时关闭过渡、强制样式落定、再交还过渡；改为直接写 DOM，因为 React 状态不保证何时到达 DOM |

另外发现并修复：**行进高光会整圈乱转**。虚拟光源角度取自 `atan2`，指针越过左边缘时跳变一整圈，而这个角度带 220ms 过渡，于是高光每次都在同一个位置绕轮廓飞一圈——固定在同一处发生，正是它看起来像故障而不像光的原因。现在角度连续累计。

八条回归用例落在 `tests/browser/gesture.spec.ts`，其中「交叉淡出」一条断言任意时刻高亮块与融合层的不透明度之和恒为一（有凹陷就是闪烁，超过一就是两块）。

---

## 一、alpha.2 修掉的问题

### 用户报告的缺陷

| # | 现象 | 根因 | 处理 |
| --- | --- | --- | --- |
| 1 | 左栏高亮一直停在第一项 | 选中透镜只写了水平位移；纵向侧边栏里每一项的水平位置都是 0 | 两个轴都跟踪；新增回归用例断言透镜顶边与当前项对齐 |
| 2 | 深色模式下对话框是白的 | 演示框把外观写死成浅色，而 `<dialog>` 走浏览器顶层渲染，白色面板盖在深色页面上 | 演示框跟随站点外观，用户手动切换后才固定；新增三种浮层的深色断言 |
| 3 | 输入框默认高亮，边框丑且重叠 | 全局规则与字段容器各画了一个 3px 环，且用 `:focus-within`（指针点击和自动聚焦都会亮） | 统一为一个 2px 环，改用 `:focus-visible`；库内以足够的优先级压住输入框自身的环，应用样式表的加载顺序无法破坏它 |
| 4 | GlassSheet 没有开关动画 | 缺 `@starting-style` 与 `allow-discrete`，背景遮罩也没有过渡 | 补齐升起/落下与遮罩淡入淡出；减少动效时直接切换 |
| 5 | 组件演示会改变整站路由 | 列表行与标签栏演示用的是真实跳转链接 | 统一为不跳转的演示链接；新增用例逐个点过去断言地址栏不变 |
| 6 | 演示页形态、左栏命名 | 单个大演示块，左栏只有英文名 | 改为多示例卡片 + 右侧目录；全站统一「中文 英文」 |
| 7 | 文案有开发术语 | 站点文案写的是实现与验证过程 | 全站重写为面向使用者的说明；移除三个实验室页面 |

### 审查中另外发现的

| # | 严重度 | 问题 | 处理 |
| --- | --- | --- | --- |
| A | Major | 首屏主题脚本是内联的，违反预览服务器的 CSP | 加上 nonce 占位；用例断言零违规 |
| B | Major | 打包会丢掉 `"use client"`，导致包无法在 Next.js App Router 的服务端组件里被导入 | 产物顶部统一加一条；发版检查里明确要核对 |
| C | Minor | 触摸端补足命中区的伪元素同时写了 `inset`、`inset-inline`、`translate` 和 `transform`，相互冲突 | 重写为从中心扩展的单一写法 |
| D | Minor | 材质的模糊、饱和度、高光角度在 CSS 与 TypeScript 里各声明了一份，改 CSS 那份不生效 | 删除重复项，注明唯一来源 |
| E | Minor | 演示侧栏行 40px、文档左栏 34px，低于触摸端下限 | 触摸设备上统一补到 44 |

---

## 二、逐组件结论

全部 27 个组件页都通过了同一套基线检查（页面完整、无控制台报错、无横向溢出、示例可展开代码、属性表与键盘说明齐全）。下面只记录每个组件在设计规则上的关键点与结论。

| 组件 | 关键规则 | 结论 |
| --- | --- | --- |
| 文本 Text | 用内建样式建立层级；11px 是下限；标题层级不推断 | ✓ 全站最小字号断言为 11px |
| 卡片 Card | 内容层不用玻璃；嵌套圆角同心 | ✓ 用例断言卡片不采样背景 |
| 列表 List | 可点的行是真链接或按钮；分区标题不全大写；分隔线内缩 | ✓ |
| 标准材质 MaterialView | 内容层的半透明手段，不带折射与高光边 | ✓ 用例断言无 rim、无装饰层 |
| 分隔线 Divider | 栏之间不用它 | ✓ |
| 按钮 GlassButton | 一屏一个主操作；触摸端命中区 44 | ✓ 用例断言每个示例只有一个 prominent |
| 分段控件 SegmentedControl | 可拖动，拖动中即时切换；原生 radio | ✓ 透镜在轨道内 1:1 跟手，出界才变沉 |
| 开关 GlassSwitch | 可甩，方向决定结果；打开态系统绿 | ✓ 旋钮在 20px 行程内 1:1 跟手 |
| 滑块 GlassSlider | 旋钮静止安静，仅拖动时抬升为玻璃 | ✓ 用例断言静止态装饰不可见 |
| 步进器 GlassStepper | 两个具名按钮，不是数字框 | ✓ |
| 进度 GlassProgress | 能算出进度就不要转圈；减少动效时停止 | ✓ |
| 徽标 GlassBadge | 颜色不是唯一信息；需要说明数什么 | ✓ |
| 输入框 TextField | 真实标签；错误不只靠红框；≥16px 防缩放 | ✓ |
| 搜索框 SearchField | 独立玻璃面；焦点环在容器上 | ✓ |
| 工具栏 GlassToolbar | 栏无背景，分组才是玻璃；图标与文字不同组 | ✓ 混排时开发模式告警 |
| 标签栏 TabBar | 是 nav 不是 tablist；搜索独立一格；宽屏变侧边栏 | ✓ |
| 侧边栏 Sidebar | 大玻璃更厚且不翻转明暗 | ✓ |
| 标签页 GlassTabs | 就地换内容，与页面导航区分 | ✓ |
| 导航栏 NavigationBar | 大标题在场时紧凑标题隐藏 | ✓ |
| 滚动边缘 ScrollEdge | 渐进溶解而非色块；一个滚动区一个 | ✓ |
| 气泡面板 GlassPopover | 从触发控件长出并锚定；Escape 回焦 | ✓ |
| 菜单 GlassMenu | 完整键盘模型；每组约七项 | ✓ |
| 底部面板 GlassSheet | 拖动跟手、弹簧停靠、满高变不透明 | ✓ 本轮补上进出动画 |
| 警告框 GlassAlert | 标题加粗左对齐；破坏性时焦点在取消；Escape 等于取消 | ✓ |
| 操作表 GlassActionSheet | 破坏性排最后，取消分开 | ✓ |
| 对话框 GlassDialog | 原生 dialog 提供焦点约束与背景隔离 | ✓ |
| 轻提示 Toast | 可逆操作给撤销而不是弹窗；悬停暂停计时 | ✓ |

## 三、系统偏好

浏览器内实测（通过站点自带的偏好开关，叠加在系统设置之上）：

| 设置 | 预期 | 实测 |
| --- | --- | --- |
| 默认 | 行进高光 + 背景模糊 | 圆锥渐变描边，`blur(14px) saturate(…)` |
| 增强对比度 | 接近黑白 + 对比边框，关掉折射 | 描边变实色边框，背景滤镜为 `none` |
| 减少透明度 | 背景滤镜关闭，材质变实 | 背景滤镜为 `none`，边缘保留 |
| 减少动效 | 关闭弹性、位移、拖动形变 | 用例断言分段控件不再拉伸 |
| 强制颜色 | 交给系统调色板 | 用例断言折射路径关闭 |

## 四、长期挂账的六项：现状

| # | 状态 | 说明 |
| --- | --- | --- |
| R1 | **仍未做** | 屏幕阅读器实机验证。自动化能证明角色、名称和键盘路径是对的，证明不了朗读顺序读起来是否自然。没有办法绕过。 |
| R2 | **已关闭** | 玻璃在真实合成背景上的对比度。见下。 |
| R3 | **已关闭（自动化部分）** | WebKit 与 Firefox 各 8 条用例，`pnpm test:fallback`。人眼确认退化观感仍然建议做一次。 |
| R4 | **已关闭** | 公开运行时名字从 75 个收敛到 69 个。 |
| R5 | **已量到** | 数字见下；CPU 节流不等于低端设备。 |
| R6 | **已关闭（模拟部分）** | 4 条真实 touch 事件用例。模拟解决不了「手感」，但它确实定死了由代码决定的那些部分。 |

### R2 — 对比度，从合成后的像素上量

把字形藏起来、截下控件、把 PNG 交回页面里解码，读玻璃最终合成成了什么颜色。文字颜色本身是不透明的、已知的，所以唯一的未知数——玻璃压在那张图上变成了什么——正是被量出来的那一半。工具：`scripts/measure-contrast.mjs`。

**量出来是不合格的。** 媒体场景上的控件，最差的一块：

| | Chrome | WebKit | Firefox |
| --- | --- | --- | --- |
| 修之前 | 4.38:1 **不合格** | 5.69:1 | 4.42:1 **不合格** |
| 修之后 | 7.83:1 | 9.47:1 | 7.83:1 |

原因不在库里，在文档站的声明：那两组控件写的是 `backdropTone="dark"`，而控件底下那片场景实测中位亮度 152、亮部 237——那是浅色背景。声明错了，clear 材质就只上了 6% 的压暗，白色图标压在亮部上掉到 4.38。改成 `light` 之后 clear 会上 35% 的黑色压暗，这正是 HIG 对 clear 的要求（「只用在明亮的媒体上，并且要压暗」）。

`tests/browser/contrast.spec.ts` 把这条守住了：同样的量法，最差的一块不得低于 4.5。

### R3 — 顺带量出来一个真问题

`CSS.supports('backdrop-filter', 'url(#id)')` 在 **Chromium、WebKit、Gecko 三家都返回 true**。它检的是语法，不是能力，所以它分不出「会渲染」和「只是能解析」。而库拿它当能力开关用。

后果：任何设了 `renderer="svg"` 的应用，在 Safari 和 Firefox 上会走进 SVG 分支，而 `--lg-backdrop` 里那一整条当时只有 `url(...)`——玻璃不是退化成磨砂，是退化成一块透明的洞。文档却承诺「Safari 和 Firefox 退化成磨砂玻璃」。

处理：把模糊从 SVG 滤镜里搬到 CSS 链的最前面（`blur(N) url(#id) saturate(...)`），滤镜只负责位移。哪家引擎悄悄丢掉 `url()`，剩下的仍然是一层真的磨砂。已在 WebKit 与 Chrome 上各看过一眼确认。

顺带记下：SVG 分支此前**没有任何测试或页面在跑它**——`enableSvgAuto` 默认关，站点也没开过。一个有文档、没人走过的分支。

### R5 — 性能，换成量出来的数

`scripts/measure-performance.mjs`，在最重的那一页上拖动，取浏览器自己的帧间隔：

| CPU | 玻璃面 | 中位帧 | p95 | 超过 32ms 的帧 |
| --- | --- | --- | --- | --- |
| 1× | 11 | 16.7ms | 16.7ms | 1 / 117 |
| 4× | 11 | 16.7ms | 16.7ms | 0 / 118 |
| 6× | 11 | 16.7ms | 16.8ms | 1 / 119 |

11 块玻璃在 6 倍 CPU 节流下仍然是满帧。**但这不等于「二十块也行」，更不等于低端设备也行**：节流只拖慢 CPU，不动 GPU，而 backdrop-filter 花的恰恰是填充率和带宽。这组数只能当下限读。

## 五、结论

两轮下来，用户报告的十三个问题全部修复，各自有回归用例守住；审查中另外发现并修复了六个，其中 CSP 违规和丢失 `"use client"` 会直接导致包在真实项目里用不了，行进高光整圈乱转则是一眼可见的故障感。

最能改变观感的，第一轮是**把左栏高亮修对**、**演示页改成可查阅的形态**、**文案换成人话**；这一轮是**让控件真的被手指带着走**——在此之前每个「可拖动」的控件都只是朝手指歪了十几像素，这恰恰是一套界面「差那么一点」最容易被感觉到、最难被截图证明的地方。
